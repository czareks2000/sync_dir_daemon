/**
 * Fukncja sprawdzająca czy podana ścieżka jest katalogiem.
 * 
 * @param path Ścieżka do katalogu.
 * 
 * @return 0 jeżeli podana ścieżka jest katalogiem, <0 w przypadku wystąpienia błędu
 */
int isDir(const char *path);